name: Deploy Backend to Railway

on:
  workflow_dispatch:
  push:
    paths:
      - 'server.js'
      - 'package.json'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Railway
        run: |
          echo "Deploy your backend separately to Railway, Render, or Fly.io"
          echo "See DEPLOYMENT.md for instructions"

